package com.angelokezimana.posta.dto.blog;

public record AuthorDTO(Long id,
                        String firstName,
                        String lastName,
                        String email) {
}
