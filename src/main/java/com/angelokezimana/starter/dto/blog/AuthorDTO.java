package com.angelokezimana.starter.dto.blog;

public record AuthorDTO(Long id,
                        String firstName,
                        String lastName,
                        String email) {
}
