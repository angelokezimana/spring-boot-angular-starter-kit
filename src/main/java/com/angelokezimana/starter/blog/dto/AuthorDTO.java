package com.angelokezimana.starter.blog.dto;

public record AuthorDTO(Long id,
                        String firstName,
                        String lastName,
                        String email) {
}
